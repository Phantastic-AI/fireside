{
  "name": "fireside",
  "version": "0.1.0",
  "private": true,
  "description": "An open-source call for speakers that remembers who you have told.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "bash scripts/deploy.sh",
    "test": "vitest run",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "fflate": "^0.8.3",
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260804.1",
    "typescript": "^5.6.0",
    "wrangler": "^4.121.0"
  }
}
